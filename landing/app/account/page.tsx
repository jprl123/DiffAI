// English alias for the license portal. The canonical route stays /conta
// because the Stripe SUCCESS_URL configured on Railway points there — both
// paths render the same page.
export { default } from "../conta/page"
